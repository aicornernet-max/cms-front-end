/**
 * This feature module expects a pre-configured axios instance to already exist
 * in your project (baseURL = e.g. http://localhost:5000/api, withCredentials, etc).
 *
 * Adjust the relative path below to point at your real instance. Nothing else
 * in this feature needs to change.
 */
import axiosInstance from "../../../api/axios";

export default axiosInstance;
